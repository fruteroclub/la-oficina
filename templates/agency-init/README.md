# {{AGENCY_NAME}} — agency root

This directory is the agency root, scaffolded by La Oficina.

- `.obsidian/` — LLM-Wiki vault
- `.oficina/` — La Oficina marker; see `config.yaml` and `pods.yaml`
- `pods/` — operating system units

Run `oficina add-pod <name>` to create your first pod.
