# CLAUDE.md — office ({{POD_NAME}})

Thin Claude Code pointer. Canonical doctrine: `AGENTS.md`.

## Essentials
1. Read `AGENTS.md` first
2. Use `oficina resolve` and `oficina module-load <path>`
3. Office orchestration layer; workspaces in `workspaces/`
4. Daily narrative in `journal/YYYY-MM-DD.md`
5. Write scope per AGENTS; pod-level off-limits
